# WordPress GraphQL endpoint
WPGRAPHQL_URL=https://your-wordpress-site.com/graphql

# HTTP Basic auth credentials for a password-protected WordPress instance.
# Sent as an `Authorization: Basic` header by gatsby-source-wordpress.
WP_BASIC_AUTH_USER=
WP_BASIC_AUTH_PASSWORD=
